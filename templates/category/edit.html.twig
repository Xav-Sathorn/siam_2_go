{% extends "base.html.twig" %}

{% block title %}
	Modification de
	{{category.name}}
{% endblock %}

{% block body %}
	<h1>Modifier
		{{category.name}}</h1>
	{{form_start(formView)}}

	{{form_widget(formView)}}

	<button type="submit" class="btn btn-primary mt-4">
		<i class="fas fa-save">
			Enregistrer
		</i>
	</button>

	{{form_end (formView)}}
{% endblock %}
